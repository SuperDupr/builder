<h1>Industries#new</h1>

<%= render(partial: "form", locals: {industry: @industry, url: account_industries_path(account_id: current_account.id)}) %>

<%= link_to("Back to Industries", account_industries_path(account_id: current_account.id)) %>