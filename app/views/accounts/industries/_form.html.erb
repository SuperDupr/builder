<%= form_for(industry, url: url) do |f| %>
  <div class='flex place-items-end gap-2'>
    <div class="">
     <%= f.label("Title") %>
      <%= f.text_field(:title, required: true, class: "form-control") %>
    </div>
    <%= f.submit("Save", class: "btn btn-primary") %>
  </div>
<% end %>
