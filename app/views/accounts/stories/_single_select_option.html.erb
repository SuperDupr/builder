<label for="<%= node.id %>" class="flex gap-1">
  <input data-checkbox-target="checkbox" id="<%= node.id %>" type="checkbox" class="mr-1 mt-[2px] nodes" value="<%= node.title %>" <%=  prompt.selector&.include?(node.title) ? 'checked' : '' %> data-action="change->stories#disableNavigationButtonsOnChange">
  <%= node.title %>
</label>