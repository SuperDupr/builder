<%= prompt.selector %>
<label for="<%= node.id %>" class="mb-2 flex gap-1">
  <input data-checkbox-target="checkbox" id="<%= node.id %>" type="checkbox" class="mr-1 mt-[2px] nodes" value="<%= node.title %>" <%= node.title == prompt ? 'checked' : '' %> data-action="change->stories#disableNavigationButtonsOnChange">
  <%= node.title %>
</label>