<label for="<%= node.id %>" class="flex gap-1">
  <input data-checkbox-target="checkbox" id="<%= node.id %>" type="checkbox" class="mr-1 mt-[2px] nodes" value="<%= node.id %>" <%=  prompt.selector&.include?(node.id.to_s) ? 'checked' : '' %>>
  <%= node.title %>
</label>