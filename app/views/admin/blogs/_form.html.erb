<%= form_with(model: blog, url: url, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
        <% blog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, required: true %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.rich_text_area :body %>
  </div>

  <!-- TODO: Add field for published boolean. Discard it if not included in the final UI -->
  <!-- <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published %>
  </div> -->

  <!-- <div class="field">
    <%#= form.label :tag_list, "Tags (comma-separated)" %>
    <%#= form.text_field :tag_list, placeholder: "Tag1, Tag2, Tag3" %>
  </div> -->

  <div class="actions">
    <%= form.submit("Save", class: "btn btn-primary") %>
  </div>
<% end %>
